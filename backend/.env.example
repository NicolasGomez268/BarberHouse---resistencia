# Permitir requests del frontend (dev: localhost:5173, prod: tu dominio)
CORS_ORIGIN=http://localhost:5173

# NOTA: No uses prefijos reservados en este archivo:
# FIREBASE_*, GOOGLE_*, EXT_* son inyectados automaticamente por el emulador/Cloud Functions.
# En produccion las credenciales vienen del service account del entorno, no de este archivo.
